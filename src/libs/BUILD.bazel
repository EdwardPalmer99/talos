cc_library(
    name="order_management_system_lib",
    hdrs=glob(["*.hpp", "**/*.hpp"]),
    srcs=glob(["*.cpp", "**/*.cpp"]),
    includes=["."],
    visibility=["//visibility:public"]
)

# TODO: - create libraries for each directory